/* Operadores Lógicos */

void main() {
  bool r = 10 == 28 || 10 != 5 || 15 == 10;

  // == Igualdade
  // != Diferença
  // > Maior que
  // < Menor que
  // >= Maior ou igual a
  // <= Menor ou igual a
  // || OR
  // && AND
  // ! NOT

  print(!r);
}
